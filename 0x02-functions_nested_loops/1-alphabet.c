#include "main.h"

/**
 * print_alphabet - prints the alphabet in lowercase, followed by a new line.
 */
void print_alphabet(void)
{
	char each_letter;

	for (each_letter = 'a'; each_letter <= 'z'; each-leter++)
		_putchar(each_letter);

	_putchar('\n');


}

